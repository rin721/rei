# ADR 001: 初始化 Vibe Memory Base

日期：2026-04-20

## 背景

项目此前只有一份单文件工程协议，难以承载持续演进中的架构规则、当前任务与历史决策。

## 决策

今天将单文件 IEP 升级为目录化的 Agent 记忆底座，建立 `docs/vibe/` 作为长期上下文中心。

## 结果

1. 使用 `constitution/` 承载不可违背的工程规则。
2. 使用 `state/` 承载当前阶段性目标。
3. 使用 `adr/` 记录关键演进决策。
4. 为后续持续演进的架构重构提供稳定、可扩展、物理隔离的记忆基础。
