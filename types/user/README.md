# User Types

This package now contains the shared DTOs used by auth and user handlers, including:

- register and login requests
- password change and refresh-token requests
- profile responses
- token-pair responses
